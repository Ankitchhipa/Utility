<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <include android:id="@+id/toolbar"
            layout="@layout/main_toolbar_apc" />

        <LinearLayout
            android:id="@+id/juncscreen_toplayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/sidebar_header_color"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="@dimen/activity_horizontal_marginbig">

            <TextView
                android:id="@+id/tv_noticount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/colorPrimaryDark"
                android:textSize="@dimen/_20ssp"
                app:fontFamily="@font/roboto_medium" />

            <TextView
                android:id="@+id/tvnoti_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:includeFontPadding="false"
                android:text="@string/str_app_notification"
                android:textColor="@color/text_color"
                android:textSize="@dimen/_14ssp"
                app:fontFamily="@font/roboto_regular" />
        </LinearLayout>

        <include layout="@layout/notification_activity" />
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
